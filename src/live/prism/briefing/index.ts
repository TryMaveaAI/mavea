// briefing/ — The Briefing: a captioned, camera-led flight along the document's argument (narrated
// by default when the reader asked for it; silent under the tour, which narrates over its own flight),
// built entirely from grounded claims/threads/verdicts (no model call). Public surface: the player
// component + the pure path-builder.

export { buildBriefing } from './path';
